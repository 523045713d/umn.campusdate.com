"use client";import {useEffect,useMemo,useState} from "react";import {PlanCard} from "@/components/PlanCard";import {loadPlans} from "@/lib/store";import type {Plan,PlanCategory} from "@/types";
const categories:Array<"All"|PlanCategory>=["All","Study","Food","Sports","Event","Build"];
export default function DiscoverPage(){const[plans,setPlans]=useState<Plan[]>([]);const[category,setCategory]=useState<(typeof categories)[number]>("All");useEffect(()=>setPlans(loadPlans()),[]);const filtered=useMemo(()=>category==="All"?plans:plans.filter(p=>p.category===category),[plans,category]);return <section><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h1 className="text-4xl font-semibold tracking-tight">Discover Plans</h1><p className="mt-3 text-neutral-600">Browse first. Match automatically only when it helps.</p></div><div className="flex flex-wrap gap-2">{categories.map(item=><button key={item} onClick={()=>setCategory(item)} className={`rounded-full px-4 py-2 text-sm ${category===item?"bg-black text-white":"border border-black/10 bg-white"}`}>{item}</button>)}</div></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(plan=><PlanCard key={plan.id} plan={plan}/>)}</div></section>}
